export const webhook = "https://discord.com/api/webhooks/WEBHOOK_ID_HERE/";
export const token = "WEBHOOK_TOKEN_HERE";
export const includeRepos: Array<string> = []; // empty to include all by default, add items to exclude all by default
export const excludeRepos: Array<string> = ["repo-to-ignore"];
